# Verification path

The user selected Horizonte digital. The three-option surface round discharged composition selection; no additional round is owed.

The measured comp spec and original 1536 × 1024 image remain fidelity references. The automatic font-ranking gate cannot proceed: the binary reports neither a Playwright/Puppeteer browser nor a catalog index. Browser interaction in this session uses CUA. Do not install or switch to another browser technology to satisfy that binary. Archivo and Commit Mono are explicitly pinned by the user; retain them and compare cap-height and layout in CUA captures. The build state records this unavailable gate without a fabricated pass or forced advance.

Countable functional diagrams are semantic SVG, as requested by the user and permitted by visualize.md. They are labelled representations. The independent reviewer receives this exception and the CUA screenshots. No private-product screenshots or invented empirical claims are used.

The latest user instruction replaces the live hero raster and image particle reveals with one fixed procedural cloud that changes geometry and position with native scroll. The raster remains a functional failure fallback. GSAP ScrollTrigger and Tailwind are now requested. This is an authorized medium adaptation, not an automatic phase advance. Current evidence and limits are in `.impeccable/review/validation.md` and `manual-closure.json`.
